version: '3'

services:
  backend:
    build:
      context: ./Back-End
      dockerfile: Dockerfile
    image: hotelcombiend/my-backend_image:1.0
  frontend:
    build:
      context: ./Front-End
      dockerfile: Dockerfile
    image: hotelcombiend/my-frontend_image:1.0
